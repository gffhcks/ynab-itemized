# YNAB Itemized - Requirements File
#
# This project uses UV for package management.
# For locked dependencies, see:
#   - requirements.lock (production dependencies)
#   - requirements-dev.lock (development dependencies)
#
# To install with UV:
#   uv pip install -r requirements.lock
#   uv pip install -r requirements-dev.lock  # for development
#
# To regenerate lock files:
#   uv pip compile pyproject.toml -o requirements.lock
#   uv pip compile pyproject.toml --extra dev -o requirements-dev.lock
#
# For traditional pip installation (not recommended):
#   pip install -e .
#   pip install -e ".[dev]"  # for development

# YNAB API Integration
requests>=2.31.0
pynab>=0.6.8

# Database and Data Storage
sqlalchemy>=2.0.0
alembic>=1.12.0

# Data Processing and Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
pandas>=2.0.0
numpy>=1.24.0

# Configuration Management
python-dotenv>=1.0.0
pyyaml>=6.0

# Logging and Utilities
structlog>=23.0.0
click>=8.1.0
rich>=13.0.0

# Date/Time Handling
python-dateutil>=2.8.0

# Platform Utilities
platformdirs>=3.0.0
